import { BackgroundPaths } from "@/components/ui/background-paths";

const Cta = () => {
  return (
    <section id="cta">
      <BackgroundPaths title="Background Paths" />
    </section>
  );
};

export default Cta;
